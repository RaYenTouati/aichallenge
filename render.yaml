services:
  - type: web
    name: unihelp-api
    env: python
    buildCommand: "pip install -r requirements.txt && python generate_pdf.py && python -m app.indexer"
    startCommand: "uvicorn app.api:app --host 0.0.0.0 --port 10000"
    envVars:
      - key: PYTHON_VERSION
        value: 3.10
